# VoidedClient 1.10.0 — Velocity Server Browser

- Keeps client version 1.10.0.
- Server Browser now understands backend availability.
- Offline servers display `OFFLINE` and cannot be clicked.
- Player counts come from Velocity proxy snapshots rather than backend-only heartbeats.
- Parser remains backward-compatible with older four-field `SERVER_LIST` packets.
