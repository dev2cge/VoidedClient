# VoidedClient 1.10.0 — Network HUD Build Fix

- Fixed `ServerContextState` record compilation on GitHub Actions.
- Renamed static helper methods from `rpgEnabled()` / `gameplayEnabled()` to
  `isRpgEnabled()` / `isGameplayEnabled()`.
- Record component accessors retain their normal instance names.
- No protocol/version change; client remains 1.10.0.
